
![This is a textual document outlining criteria for determining if external document retrieval (e.g., from Wikipedia) improves AI-generated responses to instructions. It consists of a header titled “Instructions,” followed by “Demonstrations” with multiple examples. Each example shows an instruction (e.g., *“Give three tips for staying healthy”*), a judgment for retrieval (*[Yes]* or *[No]*), and an explanation. The text is set on a white background with black typographic text, organized into clearly separated instructional blocks. Visual elements include section headings, bullet points for instructions, and structured responses for each query. The document’s purpose is to provide guidance on when retrieving external sources is helpful for generating accurate answers.](images/page_26_pic_1.png)

**Caption:** Table 8: Instructions and demonstrations for Retrieve aspect given the input only.
**Description:**
This is a textual document outlining criteria for determining if external document retrieval (e.g., from Wikipedia) improves AI-generated responses to instructions. It consists of a header titled “Instructions,” followed by “Demonstrations” with multiple examples. Each example shows an instruction (e.g., *“Give three tips for staying healthy”*), a judgment for retrieval (*[Yes]* or *[No]*), and an explanation. The text is set on a white background with black typographic text, organized into clearly separated instructional blocks. Visual elements include section headings, bullet points for instructions, and structured responses for each query. The document’s purpose is to provide guidance on when retrieving external sources is helpful for generating accurate answers.
